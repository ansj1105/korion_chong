openapi: 3.0.3
info:
  title: KORION Chong API
  version: 0.1.0
paths:
  /api/auth/login:
    post:
      summary: Authenticate an approved leader, partner, or merchant account
      description: Verifies users credentials and returns role-scoped routing metadata. It does not create authority from signup applications and does not issue a session token until session policy is finalized.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login succeeded for the selected approved role.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
  /api/auth/availability:
    get:
      summary: Check signup field availability
      parameters:
        - name: field
          in: query
          required: true
          schema:
            type: string
            enum:
              - loginId
              - email
              - telegram
              - whatsapp
              - walletAddress
        - name: value
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Availability result for frontend modal/toast handling.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailabilityResponse'
  /api/auth/referral-codes/{code}/validate:
    get:
      summary: Validate a leader or partner referral code
      parameters:
        - name: code
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Referral code validation result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReferralCodeValidationResponse'
  /api/auth/email-verifications/send:
    post:
      summary: Send distributor signup email verification code
      description: Stores only a code hash in distributor_signup_email_verifications. The raw code must be delivered by the configured mail adapter.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmailVerificationSendRequest'
      responses:
        '200':
          description: Verification code was created for delivery.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailVerificationSendResponse'
  /api/auth/email-verifications/confirm:
    post:
      summary: Confirm distributor signup email verification code
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EmailVerificationConfirmRequest'
      responses:
        '200':
          description: Email verification result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailVerificationConfirmResponse'
  /api/auth/wallet-links/verify:
    post:
      summary: Verify KORION WALLET TRX address ownership for signup
      description: Stores only a signature hash and verification result in distributor_signup_wallet_verifications.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WalletLinkVerifyRequest'
      responses:
        '200':
          description: Wallet verification result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WalletLinkVerifyResponse'
  /api/auth/signup-applications:
    post:
      summary: Submit public partner or merchant signup application
      description: Creates a partner_applications row only. It does not create users, partners, merchants, or login authority.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SignupApplicationRequest'
      responses:
        '200':
          description: Signup application submitted for review.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SignupApplicationResponse'
  /api/leader/dashboard:
    get:
      summary: Country leader dashboard summary
      parameters:
        - $ref: '#/components/parameters/LeaderIdHeader'
        - $ref: '#/components/parameters/CountryScopesHeader'
        - name: period
          in: query
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
          example: '2026-06'
        - name: countryScope
          in: query
          required: true
          schema:
            type: string
            minLength: 2
            maxLength: 2
          example: KR
      responses:
        '200':
          description: Dashboard summary scoped to the authenticated country leader.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaderDashboardResponse'
        '403':
          $ref: '#/components/responses/ForbiddenCountryScope'
  /api/leader/partners:
    get:
      summary: Approved sales partners under a country leader
      parameters:
        - $ref: '#/components/parameters/LeaderIdHeader'
        - $ref: '#/components/parameters/CountryScopesHeader'
        - name: countryScope
          in: query
          required: true
          schema:
            type: string
            minLength: 2
            maxLength: 2
          example: KR
        - name: keyword
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum:
              - SALES_PARTNER_APPROVED
        - name: region
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        '200':
          description: Approved sales partner page.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaderPartnerResponse'
        '403':
          $ref: '#/components/responses/ForbiddenCountryScope'
components:
  parameters:
    LeaderIdHeader:
      name: X-Leader-Id
      in: header
      required: true
      schema:
        type: integer
        format: int64
      description: Temporary leader partner id until JWT auth is wired.
    CountryScopesHeader:
      name: X-Country-Scopes
      in: header
      required: true
      schema:
        type: string
      description: Temporary comma-separated country scopes until JWT auth is wired.
  responses:
    ForbiddenCountryScope:
      description: Requested countryScope is outside the leader's server-side country scope.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiErrorResponse'
  schemas:
    LoginRequest:
      type: object
      required:
        - loginId
        - password
        - role
      properties:
        loginId:
          type: string
          maxLength: 50
        password:
          type: string
          format: password
          minLength: 8
          maxLength: 72
          writeOnly: true
        role:
          type: string
          enum:
            - LEADER
            - PARTNER
            - MERCHANT
        requestId:
          type: string
          maxLength: 128
    LoginResponse:
      type: object
      properties:
        authenticated:
          type: boolean
        userId:
          type: integer
          format: int64
        role:
          type: string
          enum:
            - LEADER
            - PARTNER
            - MERCHANT
        partnerId:
          type: integer
          format: int64
          nullable: true
        merchantId:
          type: integer
          format: int64
          nullable: true
        countryScopes:
          type: array
          items:
            type: string
        redirectPath:
          type: string
          enum:
            - /leader/dashboard
            - /partner/dashboard
            - /merchant/dashboard
        requiresTwoFactor:
          type: boolean
          description: false until the 2FA method and challenge contract are finalized.
        sessionExpiresAt:
          type: string
          format: date-time
          nullable: true
          description: null until session issuance policy is finalized.
        resultCode:
          type: string
          enum:
            - LOGIN_SUCCESS
        messageKey:
          type: string
    AvailabilityResponse:
      type: object
      properties:
        available:
          type: boolean
        field:
          type: string
        resultCode:
          type: string
          enum:
            - AVAILABLE
            - DUPLICATE
        messageKey:
          type: string
    ReferralCodeValidationResponse:
      type: object
      properties:
        valid:
          type: boolean
        code:
          type: string
        codeType:
          type: string
        ownerPartnerId:
          type: integer
          format: int64
        country:
          type: string
        city:
          type: string
        resultCode:
          type: string
        messageKey:
          type: string
    SignupApplicationRequest:
      type: object
      required:
        - applicantType
        - loginId
        - password
        - email
        - companyName
        - contactName
      properties:
        applicantType:
          type: string
          enum:
            - PARTNER
            - MERCHANT
        loginId:
          type: string
          maxLength: 50
        password:
          type: string
          format: password
          minLength: 8
          maxLength: 72
          writeOnly: true
          description: Raw password is accepted only for signup submission and stored as password_hash on the inactive application record.
        email:
          type: string
          format: email
          maxLength: 255
          description: Must have a VERIFIED row in distributor_signup_email_verifications before submission.
        companyName:
          type: string
          maxLength: 160
        contactName:
          type: string
          maxLength: 120
        phone:
          type: string
          maxLength: 80
        telegram:
          type: string
          maxLength: 120
        whatsapp:
          type: string
          maxLength: 80
        referralCode:
          type: string
          maxLength: 64
        country:
          type: string
          pattern: '^[A-Z]{2}$'
        region:
          type: string
          maxLength: 100
        city:
          type: string
          maxLength: 100
        address:
          type: string
          maxLength: 1000
        businessType:
          type: string
          maxLength: 80
        walletAddress:
          type: string
          description: TRON address. Stored only in partner_applications until approval.
        integrationPlan:
          type: string
          maxLength: 4000
        evidenceNote:
          type: string
          maxLength: 4000
        requestId:
          type: string
          maxLength: 128
    SignupApplicationResponse:
      type: object
      properties:
        applicationId:
          type: integer
          format: int64
        status:
          type: string
          enum:
            - REQUESTED
        resultCode:
          type: string
          enum:
            - SIGNUP_APPLICATION_SUBMITTED
        messageKey:
          type: string
        walletStored:
          type: boolean
          description: true only when the application table stores the submitted wallet address; no authority is granted.
    EmailVerificationSendRequest:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email
          maxLength: 255
        requestId:
          type: string
          maxLength: 128
    EmailVerificationSendResponse:
      type: object
      properties:
        resultCode:
          type: string
          enum:
            - EMAIL_VERIFICATION_SENT
        messageKey:
          type: string
        expiresAt:
          type: string
          format: date-time
    EmailVerificationConfirmRequest:
      type: object
      required:
        - email
        - code
      properties:
        email:
          type: string
          format: email
          maxLength: 255
        code:
          type: string
          pattern: '^\d{6}$'
        requestId:
          type: string
          maxLength: 128
    EmailVerificationConfirmResponse:
      type: object
      properties:
        verified:
          type: boolean
        resultCode:
          type: string
          enum:
            - EMAIL_VERIFIED
        messageKey:
          type: string
    WalletLinkVerifyRequest:
      type: object
      required:
        - applicantType
        - walletAddress
        - nonce
        - signature
      properties:
        applicantType:
          type: string
          enum:
            - PARTNER
            - MERCHANT
        email:
          type: string
          format: email
          maxLength: 255
        walletAddress:
          type: string
          description: KORION WALLET TRX address.
        nonce:
          type: string
          maxLength: 128
        signature:
          type: string
          maxLength: 4096
          description: Raw signature is verified but never stored; only SHA-256 hash is persisted.
        requestId:
          type: string
          maxLength: 128
    WalletLinkVerifyResponse:
      type: object
      properties:
        verified:
          type: boolean
        authStatus:
          type: string
          enum:
            - VERIFIED
        resultCode:
          type: string
          enum:
            - WALLET_VERIFIED
        messageKey:
          type: string
    ApiErrorResponse:
      type: object
      required:
        - timestamp
        - code
        - message
      properties:
        timestamp:
          type: string
          format: date-time
        code:
          type: string
        message:
          type: string
    LeaderDashboardResponse:
      type: object
      required:
        - leaderProfile
        - kpis
        - organizationSummary
        - monthlyVolume
        - feeSummary
        - riskAlerts
      properties:
        leaderProfile:
          $ref: '#/components/schemas/LeaderProfile'
        kpis:
          $ref: '#/components/schemas/LeaderKpis'
        organizationSummary:
          $ref: '#/components/schemas/OrganizationSummary'
        monthlyVolume:
          type: array
          items:
            $ref: '#/components/schemas/MonthlyVolume'
        feeSummary:
          $ref: '#/components/schemas/FeeSummary'
        riskAlerts:
          type: array
          items:
            $ref: '#/components/schemas/RiskAlert'
    LeaderProfile:
      type: object
      properties:
        leaderId:
          type: integer
          format: int64
        userId:
          type: integer
          format: int64
        loginId:
          type: string
        status:
          type: string
          enum:
            - COUNTRY_LEADER_APPROVED
        countryScopes:
          type: array
          items:
            type: string
    LeaderKpis:
      type: object
      properties:
        approvedPartnerCount:
          type: integer
          format: int64
        approvedMerchantCount:
          type: integer
          format: int64
        completedTransactionAmount:
          type: string
          format: decimal
        confirmedCommissionAmount:
          type: string
          format: decimal
    OrganizationSummary:
      type: object
      properties:
        partnerCount:
          type: integer
          format: int64
        merchantCount:
          type: integer
          format: int64
    MonthlyVolume:
      type: object
      properties:
        month:
          type: string
          example: '2026-06'
        amount:
          type: string
          format: decimal
        transactionCount:
          type: integer
          format: int64
    FeeSummary:
      type: object
      properties:
        countryLeaderFee:
          type: string
          format: decimal
        salesPartnerFee:
          type: string
          format: decimal
        korionFee:
          type: string
          format: decimal
    RiskAlert:
      type: object
      properties:
        type:
          type: string
        severity:
          type: string
        message:
          type: string
    LeaderPartnerResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PartnerSummary'
        page:
          $ref: '#/components/schemas/PageMeta'
    PartnerSummary:
      type: object
      properties:
        partnerId:
          type: integer
          format: int64
        userId:
          type: integer
          format: int64
        loginId:
          type: string
        country:
          type: string
        region:
          type: string
        city:
          type: string
        status:
          type: string
          enum:
            - SALES_PARTNER_APPROVED
        merchantCount:
          type: integer
          format: int64
        completedTransactionAmount:
          type: string
          format: decimal
        lastActivityAt:
          type: string
          format: date-time
    PageMeta:
      type: object
      properties:
        page:
          type: integer
        size:
          type: integer
        totalItems:
          type: integer
          format: int64
